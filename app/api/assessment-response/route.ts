import { NextResponse } from 'next/server'; import { submitParticipantAssessment } from '@/lib/access/assessment';
import { readJsonBody } from '@/lib/http/json';
export async function POST(request: Request) { try { const body = await readJsonBody(request); const input = (body && typeof body === 'object' ? body : {}) as Record<string, unknown>; if (typeof input.token !== 'string' || typeof input.itemId !== 'string' || (typeof input.value !== 'string' && typeof input.value !== 'number')) throw new Error('A complete assessment response is required.'); return NextResponse.json(await submitParticipantAssessment(input.token, input.itemId, input.value), { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : 'Response could not be submitted.' }, { status: 400 }); } }
